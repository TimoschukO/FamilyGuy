<div class="container">
    <h1>Регистрация и вход</h1>
    <form action="/logreg" method="post">
        <div class="form-group">
            <label for="email">Login:</label>
            <input type="text" class="form-control" id="lgn" placeholder="Логин" name="username">
        </div>
        <div class="form-group">
            <label for="pwd">Password:</label>
            <input type="Пароль" class="form-control" id="pwd" placeholder="Пароль" name="password">
        </div>
        <% if(error) { %>
        <div class="alert alert-danger"><%=error %></div>
        <% } %>
        <button type="submit" class="btn btn-default">Вход</button>
    </form>
</div>